.new-market
  %h2 Skapa ny marknad
  
  = form_for @market do |f|
    = f.label :name
    = f.text_field :name
    = f.label :description
    = f.text_area :description
    = f.submit "Skapa marknad"
