.market-form
  %h2 Skapa ny marknad
  
  = form_for @market do |f|
    .form-group
      = f.label :name, "Namn"
      = f.text_field :name, class: "form-control"

    .form-group
      = f.label :description, "Beskrivning"
      = f.text_area :description, class: "form-control"

    .form-group
      = f.submit "Skapa marknad", class: "form-control"
